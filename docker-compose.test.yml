services:
  dynamodb-local:
    image: amazon/dynamodb-local:3.0.0
    command: ["-jar", "DynamoDBLocal.jar", "-inMemory"]
    ports:
      - "8000"

  sandbox:
    depends_on:
      - dynamodb-local
